services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_NAME:-zhiyunjiaos}
      MYSQL_USER: ${DB_USERNAME:-tts}
      MYSQL_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD in .env}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?set MYSQL_ROOT_PASSWORD in .env}
    ports:
      - "${DB_PORT:-3306}:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u$$MYSQL_USER -p$$MYSQL_PASSWORD --silent"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?set REDIS_PASSWORD in .env}
    command:
      - sh
      - -c
      - exec redis-server --appendonly yes --requirepass "$$REDIS_PASSWORD"
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep PONG"]
      interval: 10s
      timeout: 5s
      retries: 12

  backend-1:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: local
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-zhiyunjiaos}
      DB_USERNAME: ${DB_USERNAME:-tts}
      DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD in .env}
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:?set REDIS_PASSWORD in .env}
      REDIS_DATABASE: ${REDIS_DATABASE:-0}
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in .env}
      TASK_WORKER_COUNT: ${TASK_WORKER_COUNT:-2}
      TASK_PER_USER_CONCURRENCY: ${TASK_PER_USER_CONCURRENCY:-2}
      TASK_GLOBAL_QUEUE_LIMIT: ${TASK_GLOBAL_QUEUE_LIMIT:-200}
      TASK_ADMISSION_RETRY_AFTER: ${TASK_ADMISSION_RETRY_AFTER:-5s}
      OBJECT_STORAGE_PROVIDER: ${OBJECT_STORAGE_PROVIDER:-aliyun-oss}
      ALIYUN_OSS_ENDPOINT: ${ALIYUN_OSS_ENDPOINT:-oss-cn-beijing.aliyuncs.com}
      ALIYUN_OSS_BUCKET: ${ALIYUN_OSS_BUCKET:?set ALIYUN_OSS_BUCKET in .env}
      ALIYUN_OSS_ACCESS_KEY_ID: ${ALIYUN_OSS_ACCESS_KEY_ID:?set ALIYUN_OSS_ACCESS_KEY_ID in .env}
      ALIYUN_OSS_ACCESS_KEY_SECRET: ${ALIYUN_OSS_ACCESS_KEY_SECRET:?set ALIYUN_OSS_ACCESS_KEY_SECRET in .env}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:8081}
      MOONSHOT_API_KEY: ${MOONSHOT_API_KEY:-}
      ALIYUN_NLS_APP_KEY: ${ALIYUN_NLS_APP_KEY:-}
      ALIYUN_AK_ID: ${ALIYUN_AK_ID:-}
      ALIYUN_AK_SECRET: ${ALIYUN_AK_SECRET:-}
      ASR_API_URL: ${ASR_API_URL:-http://host.docker.internal:9977/asr}
      TTS_API_URL: ${TTS_API_URL:-http://host.docker.internal:9880/tts}
      SOUND_CLONE_API_URL: ${SOUND_CLONE_API_URL:-http://host.docker.internal:9880/tts}
      MANAGEMENT_SERVER_PORT: 9091
      EXTERNAL_SERVICES_REQUIRED: ${EXTERNAL_SERVICES_REQUIRED:-false}
      TTS_HEALTH_URL: ${TTS_HEALTH_URL:-http://host.docker.internal:9880/tts}
      ASR_HEALTH_URL: ${ASR_HEALTH_URL:-http://host.docker.internal:9977/health}
      APP_ENVIRONMENT: ${APP_ENVIRONMENT:-docker}
      JAVA_OPTS: ${JAVA_OPTS:--XX:MaxRAMPercentage=75.0}
    ports:
      - "127.0.0.1:${BACKEND_1_PORT:-8081}:8081"
      - "127.0.0.1:${BACKEND_1_MANAGEMENT_PORT:-9091}:9091"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - backend-1-work:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9091/actuator/health >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 12
      start_period: 45s

  backend-2:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: local
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-zhiyunjiaos}
      DB_USERNAME: ${DB_USERNAME:-tts}
      DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD in .env}
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:?set REDIS_PASSWORD in .env}
      REDIS_DATABASE: ${REDIS_DATABASE:-0}
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in .env}
      TASK_WORKER_COUNT: ${TASK_WORKER_COUNT:-2}
      TASK_PER_USER_CONCURRENCY: ${TASK_PER_USER_CONCURRENCY:-2}
      TASK_GLOBAL_QUEUE_LIMIT: ${TASK_GLOBAL_QUEUE_LIMIT:-200}
      TASK_ADMISSION_RETRY_AFTER: ${TASK_ADMISSION_RETRY_AFTER:-5s}
      OBJECT_STORAGE_PROVIDER: ${OBJECT_STORAGE_PROVIDER:-aliyun-oss}
      ALIYUN_OSS_ENDPOINT: ${ALIYUN_OSS_ENDPOINT:-oss-cn-beijing.aliyuncs.com}
      ALIYUN_OSS_BUCKET: ${ALIYUN_OSS_BUCKET:?set ALIYUN_OSS_BUCKET in .env}
      ALIYUN_OSS_ACCESS_KEY_ID: ${ALIYUN_OSS_ACCESS_KEY_ID:?set ALIYUN_OSS_ACCESS_KEY_ID in .env}
      ALIYUN_OSS_ACCESS_KEY_SECRET: ${ALIYUN_OSS_ACCESS_KEY_SECRET:?set ALIYUN_OSS_ACCESS_KEY_SECRET in .env}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:8081}
      MOONSHOT_API_KEY: ${MOONSHOT_API_KEY:-}
      ALIYUN_NLS_APP_KEY: ${ALIYUN_NLS_APP_KEY:-}
      ALIYUN_AK_ID: ${ALIYUN_AK_ID:-}
      ALIYUN_AK_SECRET: ${ALIYUN_AK_SECRET:-}
      ASR_API_URL: ${ASR_API_URL:-http://host.docker.internal:9977/asr}
      TTS_API_URL: ${TTS_API_URL:-http://host.docker.internal:9880/tts}
      SOUND_CLONE_API_URL: ${SOUND_CLONE_API_URL:-http://host.docker.internal:9880/tts}
      MANAGEMENT_SERVER_PORT: 9091
      EXTERNAL_SERVICES_REQUIRED: ${EXTERNAL_SERVICES_REQUIRED:-false}
      TTS_HEALTH_URL: ${TTS_HEALTH_URL:-http://host.docker.internal:9880/tts}
      ASR_HEALTH_URL: ${ASR_HEALTH_URL:-http://host.docker.internal:9977/health}
      APP_ENVIRONMENT: ${APP_ENVIRONMENT:-docker}
      JAVA_OPTS: ${JAVA_OPTS:--XX:MaxRAMPercentage=75.0}
    ports:
      - "127.0.0.1:${BACKEND_2_PORT:-8082}:8081"
      - "127.0.0.1:${BACKEND_2_MANAGEMENT_PORT:-9092}:9091"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - backend-2-work:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9091/actuator/health >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 12
      start_period: 45s

  prometheus:
    image: prom/prometheus:v2.55.1
    profiles: ["observability"]
    restart: unless-stopped
    depends_on:
      backend-1:
        condition: service_healthy
      backend-2:
        condition: service_healthy
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=15d
    ports:
      - "127.0.0.1:${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./ops/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:9090/-/ready >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 12
      start_period: 20s

volumes:
  mysql-data:
  redis-data:
  backend-1-work:
  backend-2-work:
  prometheus-data:
